---
phase: 13-drag-interactions-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/views/MatchEditor.js
  - src/views/MatchEditor.test.js
autonomous: false
requirements:
  - DRAG-05
  - DRAG-06
  - VALID-01
  - VALID-02
  - VIS-01

must_haves:
  truths:
    - "Confirm button is disabled and styled gray when any court has exactly 1 player"
    - "Confirm button is enabled and styled blue when all courts have 0, 2, 3, or 4 players"
    - "Each invalid court has border-red-400 on its card and a visible 'needs 2+ players' label"
    - "Validation reruns reactively on every drag end (onEnd callback)"
    - "Tapping Confirm calls SessionService.updateRound(roundIndex, draft) then navigate('/active')"
    - "Tapping Cancel with no changes navigates directly to /active"
    - "Tapping Cancel with changes shows the browser confirm() dialog before navigating"
  artifacts:
    - path: "src/views/MatchEditor.js"
      provides: "validateAndUpdateUI(), handleConfirm(), handleCancel(), hasChanges()"
      contains: "validateAndUpdateUI"
    - path: "src/views/MatchEditor.test.js"
      provides: "Tests for VALID-01, VALID-02, DRAG-05, DRAG-06"
      contains: "VALID-01"
  key_links:
    - from: "SortableJS onEnd -> handleDragEnd()"
      to: "validateAndUpdateUI(el)"
      via: "called inside handleDragEnd after reconcileDraftFromDOM"
      pattern: "validateAndUpdateUI"
    - from: "#confirm-btn click"
      to: "SessionService.updateRound()"
      via: "handleConfirm() anyInvalid guard then calls updateRound"
      pattern: "updateRound"
    - from: "#cancel-btn click"
      to: "navigate('/active')"
      via: "handleCancel() checks hasChanges() then navigates or shows confirm() dialog"
      pattern: "handleCancel"
---

<objective>
Wire reactive validation (VALID-01, VALID-02), Confirm (DRAG-05, DRAG-06), and Cancel (DRAG-05, D-05, D-06) into MatchEditor, then human-verify the complete interactive editor on a real browser.

Purpose: Makes the editor functional end-to-end — the organizer can drag players, see inline error feedback, save via Confirm, or discard via Cancel.

Output:
- src/views/MatchEditor.js — validateAndUpdateUI(), handleConfirm(), handleCancel() fully implemented
- src/views/MatchEditor.test.js — green tests for VALID-01, VALID-02, DRAG-05, DRAG-06
- Human checkpoint: visually verify drag interactions, validation highlighting, bottom bar, iOS touch behavior
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-drag-interactions-validation/13-CONTEXT.md
@.planning/phases/13-drag-interactions-validation/13-RESEARCH.md
@.planning/phases/13-drag-interactions-validation/13-UI-SPEC.md
@.planning/phases/13-drag-interactions-validation/13-01-SUMMARY.md

<interfaces>
Module-scope variables set by Plan 01 (read 13-01-SUMMARY.md to confirm):

  let _sortableInstances = [];
  let _draft = null;          // deep copy rebuilt from DOM after each drag
  let _originalRound = null;  // deep copy at mount time, never mutated
  let _roundIndex = null;     // int
  let _el = null;             // the mounted element

Plan 01 defined a no-op validateAndUpdateUI(el) placeholder. Plan 02 replaces it.

SessionService.updateRound(roundIndex: number, updatedRound: object): void
navigate(path: string): void  -- navigate('/active') used by both Confirm and Cancel

Validation rules (CONTEXT.md D-03, D-04):
  isInvalid = (teamA.length + teamB.length === 1)   -- 0, 2, 3, 4 are all valid
  Invalid court card: add border-red-400, remove border-gray-200
  [data-court-error] label: remove 'hidden' class when invalid
  Confirm invalid state: disabled=true, bg-gray-300, text-gray-500, opacity-50, cursor-not-allowed
  Confirm valid state: disabled=false, bg-blue-600, text-white, shadow-lg, shadow-blue-200

Cancel behavior (CONTEXT.md D-05, D-06):
  hasChanges() = JSON.stringify(_draft) !== JSON.stringify(_originalRound)
  No changes: navigate('/active') directly
  Changes: const ok = confirm("Discard changes? Your edits won't be saved."); if (ok) navigate('/active')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validateAndUpdateUI, handleConfirm, handleCancel</name>
  <files>src/views/MatchEditor.js, src/views/MatchEditor.test.js</files>
  <read_first>
    - src/views/MatchEditor.js (post-Plan-01 state — read before editing)
    - src/views/MatchEditor.test.js (post-Plan-01 state — read before editing)
    - .planning/phases/13-drag-interactions-validation/13-01-SUMMARY.md (verify Plan 01 artifacts)
    - .planning/phases/13-drag-interactions-validation/13-RESEARCH.md (Validation Pattern, Cancel/Confirm Handler Pattern sections)
    - .planning/phases/13-drag-interactions-validation/13-UI-SPEC.md (Validation Contract, Bottom Bar Contract)
  </read_first>
  <action>
    Follow TDD: write failing tests first, then implement to make them pass.

    STEP 1 — Add tests to src/views/MatchEditor.test.js (these will be RED initially).

    Add imports at top of test file if not already present:
      import { SessionService } from '../services/session.js'

    Inside the existing Phase 13 describe block, add two new nested describe blocks:

    describe('VALID-01 + VALID-02: Validation state', () => {
      test('confirm button is enabled when all courts have 2+ players', () => {
        setupEditor(makeRoundWithBench())
        const confirmBtn = el.querySelector('#confirm-btn')
        expect(confirmBtn.disabled).toBe(false)
        expect(confirmBtn.className).toContain('bg-blue-600')
      })

      test('confirm button is disabled when a court has exactly 1 player', () => {
        const round = { index: 0, played: false, courts: [{ teamA: ['p1'], teamB: [] }], sittingOut: ['p2', 'p3', 'p4'] }
        setupEditor(round)
        const confirmBtn = el.querySelector('#confirm-btn')
        expect(confirmBtn.disabled).toBe(true)
        expect(confirmBtn.className).toContain('bg-gray-300')
      })

      test('invalid court card has border-red-400 class', () => {
        const round = { index: 0, played: false, courts: [{ teamA: ['p1'], teamB: [] }], sittingOut: ['p2', 'p3', 'p4'] }
        setupEditor(round)
        const courtCard = el.querySelector('[data-court="0"]')
        expect(courtCard.className).toContain('border-red-400')
      })

      test('invalid court shows error label (not hidden)', () => {
        const round = { index: 0, played: false, courts: [{ teamA: ['p1'], teamB: [] }], sittingOut: ['p2', 'p3', 'p4'] }
        setupEditor(round)
        const errorLabel = el.querySelector('[data-court-error]')
        expect(errorLabel.className).not.toContain('hidden')
        expect(errorLabel.textContent.trim()).toBe('needs 2+ players')
      })

      test('valid court (0 players) has border-gray-200 and error hidden', () => {
        const round = { index: 0, played: false, courts: [{ teamA: [], teamB: [] }], sittingOut: ['p1', 'p2', 'p3', 'p4'] }
        setupEditor(round)
        const courtCard = el.querySelector('[data-court="0"]')
        expect(courtCard.className).toContain('border-gray-200')
        const errorLabel = el.querySelector('[data-court-error]')
        expect(errorLabel.className).toContain('hidden')
      })
    })

    describe('DRAG-05: Confirm and Cancel wiring', () => {
      test('clicking Confirm (when valid) calls SessionService.updateRound and navigate(/active)', () => {
        vi.spyOn(SessionService, 'updateRound').mockImplementation(() => {})
        setupEditor(makeRoundWithBench())
        el.querySelector('#confirm-btn').click()
        expect(SessionService.updateRound).toHaveBeenCalledWith(0, expect.any(Object))
        expect(navigate).toHaveBeenCalledWith('/active')
      })

      test('clicking Cancel with no changes navigates directly to /active', () => {
        setupEditor(makeRoundWithBench())
        el.querySelector('#cancel-btn').click()
        expect(navigate).toHaveBeenCalledWith('/active')
      })
    })

    Run tests: npx vitest run src/views/MatchEditor.test.js -- the new tests will be RED (expected).

    STEP 2 — Implement in src/views/MatchEditor.js.

    Replace the no-op validateAndUpdateUI placeholder with this full implementation:

      function validateAndUpdateUI(el) {
        let anyInvalid = false;
        _draft.courts.forEach((court, i) => {
          const total = court.teamA.length + court.teamB.length;
          const isInvalid = total === 1;
          if (isInvalid) anyInvalid = true;
          const card = el.querySelector('[data-court="' + i + '"]');
          if (!card) return;
          card.classList.toggle('border-red-400', isInvalid);
          card.classList.toggle('border-gray-200', !isInvalid);
          const errorLabel = card.querySelector('[data-court-error]');
          if (errorLabel) errorLabel.classList.toggle('hidden', !isInvalid);
        });
        const confirmBtn = el.querySelector('#confirm-btn');
        if (confirmBtn) {
          confirmBtn.disabled = anyInvalid;
          confirmBtn.classList.toggle('opacity-50', anyInvalid);
          confirmBtn.classList.toggle('cursor-not-allowed', anyInvalid);
          confirmBtn.classList.toggle('bg-blue-600', !anyInvalid);
          confirmBtn.classList.toggle('text-white', !anyInvalid);
          confirmBtn.classList.toggle('shadow-lg', !anyInvalid);
          confirmBtn.classList.toggle('shadow-blue-200', !anyInvalid);
          confirmBtn.classList.toggle('bg-gray-300', anyInvalid);
          confirmBtn.classList.toggle('text-gray-500', anyInvalid);
        }
      }

    Add hasChanges helper:

      function hasChanges() {
        return JSON.stringify(_draft) !== JSON.stringify(_originalRound);
      }

    Add handleCancel:

      function handleCancel() {
        if (!hasChanges()) { navigate('/active'); return; }
        const confirmed = confirm("Discard changes? Your edits won't be saved.");
        if (confirmed) navigate('/active');
      }

    Add handleConfirm:

      function handleConfirm() {
        const anyInvalid = _draft.courts.some(c => (c.teamA.length + c.teamB.length) === 1);
        if (anyInvalid) return;
        SessionService.updateRound(_roundIndex, _draft);
        navigate('/active');
      }

    In mount(), after initSortables(el), add:
      validateAndUpdateUI(el);
      el.querySelector('#cancel-btn').addEventListener('click', handleCancel);
      el.querySelector('#confirm-btn').addEventListener('click', handleConfirm);

    Remove the #back-btn from the header HTML and remove its addEventListener call. The header becomes:
      header class="flex items-center space-x-4"  with only the h1 (no back button).

    Update the existing back-nav test ('Back button calls navigate(/active)') — replace it with:
      test('Cancel button calls navigate(/active)', () => {
        const session = makeSession([makeRound(0, false)])
        StorageAdapter.set('clubs', CLUBS_DATA)
        StorageAdapter.set('sessions', [session])
        mount(el, { roundIndex: '0' })
        vi.spyOn(SessionService, 'updateRound').mockImplementation(() => {})
        el.querySelector('#cancel-btn').click()
        expect(navigate).toHaveBeenCalledWith('/active')
      })

    STEP 3 — Run tests again: npx vitest run src/views/MatchEditor.test.js -- all tests must be GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/views/MatchEditor.test.js 2>&1 | tail -30</automated>
    All tests must be GREEN.
    Also check:
      grep -n "validateAndUpdateUI" src/views/MatchEditor.js   -- defined and called in mount() and handleDragEnd()
      grep -n "handleConfirm" src/views/MatchEditor.js          -- present
      grep -n "handleCancel" src/views/MatchEditor.js           -- present
      grep -n "updateRound" src/views/MatchEditor.js            -- called in handleConfirm
      grep -n "Discard changes" src/views/MatchEditor.js        -- confirm() dialog string present
  </verify>
  <done>
    - All MatchEditor tests GREEN including VALID-01, VALID-02, DRAG-05, DRAG-06
    - validateAndUpdateUI() toggles border-red-400 on invalid courts and disables/enables Confirm button
    - handleConfirm() calls SessionService.updateRound(_roundIndex, _draft) then navigate('/active')
    - handleCancel() navigates directly when no changes; shows confirm() dialog when changes exist
    - validateAndUpdateUI(el) called in mount() after initSortables(el)
    - #cancel-btn wired to handleCancel; #confirm-btn wired to handleConfirm
    - #back-btn removed from header HTML and its event listener removed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — drag interactions, validation, Confirm, Cancel on device</name>
  <files>none</files>
  <read_first>
    - .planning/phases/13-drag-interactions-validation/13-UI-SPEC.md (chip states, bottom bar spec)
    - .planning/phases/13-drag-interactions-validation/13-CONTEXT.md (D-01 through D-09)
  </read_first>
  <action>
    Run the dev server: npm run dev (or: just dev)

    Open a session with at least 5 players and navigate to the match editor by tapping "Edit" on a round card.

    Work through all 10 checkpoints listed in how-to-verify. Note any failures and report them with the resume signal.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
    Full suite must be green before beginning manual checks.
  </verify>
  <what-built>
    Interactive MatchEditor with SortableJS drag-and-drop across all court columns and the Rest Bench; swap-on-occupied-slot via Swap plugin; reactive validation (red border + error label on 1-player courts, Confirm disabled); Confirm saves via SessionService.updateRound then navigates to /active; Cancel navigates directly if unchanged or shows discard confirm() dialog if changes were made; ghost/chosen CSS during drag; fixed bottom bar with Cancel left and Confirm right.
  </what-built>
  <how-to-verify>
    Run dev server: npm run dev

    Setup: open a session with 5+ players, navigate to match editor from a proposed round (Edit button).

    Checkpoint 1 — Bottom bar (D-07, DRAG-06):
      Confirm and Cancel buttons appear in a fixed bar at the bottom, above the nav bar. No overlap. Confirm is blue if round starts valid.

    Checkpoint 2 — Drag to empty slot (DRAG-01):
      Long-press a chip ~150ms until it lifts. Drag to an empty column slot. Chip moves there.

    Checkpoint 3 — Swap on occupied slot (DRAG-02):
      Long-press a chip, drag onto another chip in a different column. Both chips swap instantly.

    Checkpoint 4 — Drag to bench (DRAG-03):
      Drag a court chip to the Rest Bench zone. Chip appears in bench; court slot empties.

    Checkpoint 5 — Drag from bench to court (DRAG-04):
      Drag a bench chip to a court column. Chip moves into the court.

    Checkpoint 6 — Validation reactive (VALID-01, VALID-02):
      Drag players until exactly 1 remains in a court (teamA + teamB = 1). Court card gets red border. "needs 2+ players" label appears. Confirm becomes gray and non-tappable. Moving that last player away (to 0) clears the error and re-enables Confirm.

    Checkpoint 7 — Ghost/chosen visual (VIS-01):
      During drag: dragged chip is semi-transparent with a dashed blue border. Destination slot highlights with a blue tint on hover.

    Checkpoint 8 — Confirm saves (DRAG-05):
      Move a player. Tap Confirm. Returns to /active. The round lineup reflects the change.

    Checkpoint 9 — Cancel no changes (DRAG-05, D-05):
      Open editor, make no moves. Tap Cancel. Navigates to /active immediately, no dialog.

    Checkpoint 10 — Cancel with changes (DRAG-05, D-06):
      Open editor, move a chip. Tap Cancel. Browser confirm() appears: "Discard changes? Your edits won't be saved." OK navigates away; Cancel stays in editor.

    iOS touch (DRAG-01 through DRAG-04):
      On iOS device or Simulator, repeat drag gestures. Touch drag works with 150ms delay before chip lifts.
  </how-to-verify>
  <done>
    All 10 checkpoints pass. iOS touch drag confirmed. Phase 13 complete.
  </done>
  <resume-signal>Type "approved" when all checkpoints pass, or describe any issues found (e.g. "bottom bar overlaps nav", "swap not working", "validation not reactive")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _draft to SessionService.updateRound() | Draft contains only internal player ID UUIDs set by the app; no user-typed strings reach this path. |
| confirm() dialog | Native browser dialog; user can only click OK or Cancel; no custom input accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | handleConfirm() bypass | mitigate | anyInvalid guard inside handleConfirm() is independent of button.disabled — dual enforcement prevents bypass via devtools |
| T-13-06 | Tampering | _draft desync from DOM | accept | reconcileDraftFromDOM() reads DOM as ground truth on every onEnd; idempotent; single-user local-first app |
| T-13-07 | Information Disclosure | Discard dialog message | accept | Native confirm() shows only the hardcoded string; no session or player data in the message |
| T-13-08 | Repudiation | Browser back / hash navigation bypasses Cancel | accept | CONTEXT.md explicitly scopes Cancel check to button press only; per-spec decision |
</threat_model>

<verification>
Full suite green before human checkpoint:
  npx vitest run

Functional grep checks:
  grep -n "border-red-400" src/views/MatchEditor.js    -- in validateAndUpdateUI
  grep -n "confirmBtn.disabled" src/views/MatchEditor.js -- set in validateAndUpdateUI
  grep -n "updateRound" src/views/MatchEditor.js         -- called in handleConfirm
  grep -n "Discard changes" src/views/MatchEditor.js     -- confirm() dialog string
  grep -n "hasChanges" src/views/MatchEditor.js          -- defined and used in handleCancel
</verification>

<success_criteria>
- All vitest tests green including VALID-01, VALID-02, DRAG-05, DRAG-06 and all pre-existing tests
- validateAndUpdateUI() toggling border-red-400 and Confirm disabled/enabled reactively on every drag
- handleConfirm() calling SessionService.updateRound(roundIndex, draft) then navigate('/active')
- handleCancel() with direct navigate when unchanged; confirm() dialog when changes exist
- VIS-01: ghost chip semi-transparent with dashed border; destination slot highlights during drag
- iOS touch drag confirmed working with 150ms delay
- All 10 human checkpoints approved
</success_criteria>

<output>
After completion, create .planning/phases/13-drag-interactions-validation/13-02-SUMMARY.md
</output>

---
context: phase
phase: 16-dark-mode-visuals-toggle
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-14T23:23:23.944Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No full-file rewrites on existing src/ — discovered when slider quick-commit gutted MemberEditor.js (183 lines deleted for a 15-line task). Use Edit (surgical diff) only, never Write on existing source files.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Full-file rewrite of src/ files | Agent reads a file and regenerates it "cleanly" for a narrow task, silently deleting prior phase work (phase-8 inline rename, pencil/trash icons all lost in slider quick-commit 044450b) | blocking | Use Edit tool (diff-based) on all existing src/ files — never Write. Check `git diff --stat` after any quick task; deletions far exceeding the task scope = stop and investigate |
| Executor worktree stomping features | 16-04 executor rewrote MemberEditor from the already-gutted slider version, compounding the loss | advisory | Run executor safety checks per CLAUDE.md after every worktree merge; verify src/ diff against prior phase work |

<current_state>
Phase 16 Wave 1 complete (plans 16-01 through 16-04 all executed and merged). Currently in Wave 2 — plan 16-05 human visual verification checkpoint. The user has been reviewing the app in dark mode and reporting issues.

Issues fixed during this session beyond the plan scope:
- MatchEditor imbalanced court validation bug (all players one side → now blocked)
- RoundDisplay Alternatives/Edit buttons white background in dark mode → fixed
- MatchEditor Cancel button white, Confirm white drop shadow → fixed
- MemberEditor pencil/trash/inline-rename/club-name-edit restored (lost in slider commit)
- Select button on alternatives page shadow-green-100 removed
- All 6 browser confirm() dialogs replaced with inline bottom-sheet modals
</current_state>

<completed_work>

Wave 1 — all 4 plans complete:
- 16-01: Dark zone chips, sortable ghost, nav bar dark classes, router toggle
- 16-02: 54 dark: classes in RoundDisplay, 22 in MatchEditor (all tests pass)
- 16-03: Appearance card (Auto/Light/Dark) in Settings, wired to ThemeService
- 16-04: SessionSetup, ClubManager, MemberEditor, Help — full dark coverage

Bug fixes committed this session:
- fix(MatchEditor): imbalanced court validation (a2c35b1's predecessor)
- fix(dark): Alternatives/Edit button backgrounds, Cancel/Confirm shadows
- fix: MemberEditor restore (pencil, trash, inline rename, club name edit)
- feat: all browser confirm() → inline bottom-sheet modals (55dd954)
</completed_work>

<remaining_work>

Plan 16-05 (Wave 2) — human visual verification checkpoint:
- Task 1 (auto): pre-flight checks — already run and passed (109 tests, all grep checks pass)
- Task 2 (human-verify): User is actively checking dark mode. Outstanding:
  - Nav bar, Settings toggle, ClubManager, MemberEditor, SessionSetup, RoundDisplay, MatchEditor, Help, reload persistence
  - User has reported several issues already fixed. May have more.
  - User needs to type "approved" or report remaining issues
  - After approval: create 16-05-SUMMARY.md, run code review, update ROADMAP/STATE, verify phase goal

After phase 16 complete:
- Run /gsd-code-review 16
- Run /gsd-verify-work 16
</remaining_work>

<decisions_made>

- Bottom-sheet modal pattern chosen for all confirm() replacements — matches existing MatchEditor discard modal style
- Destructive modals (delete club, remove member, end session, reset data) use red confirm button
- Import/overwrite modals use blue confirm button (not destructive, reversible via re-import)
- Settings uses a single shared modal with dynamic title/body/color driven by showSettingsModal()
- MemberEditor/ClubManager/RoundDisplay each have their own dedicated modal in their view's HTML
</decisions_made>

<blockers>

- Wave 2 plan 16-05 is blocked on human visual sign-off (user must type "approved")
- Large backlog of pre-existing planning file deletions in working tree (phases 08-14 .planning/ dirs show as D in git status) — these are from the slider quick-commit 044450b and were already deleted before this session; do NOT restore them as they've been archived
</blockers>

## Required Reading (in order)
1. `.planning/phases/16-dark-mode-visuals-toggle/16-05-PLAN.md` — the human verification plan with full checklist
2. `CLAUDE.md` — executor worktree safety checks (mandatory after any worktree merge)
3. `src/views/MemberEditor.js` — recently restored; confirm pencil/trash/inline-edit intact before touching

## Infrastructure State
- Dev server: NOT running (user runs `npm run dev` manually)
- All 112 tests passing as of last run
- No uncommitted src/ changes — all clean

<context>
We were executing phase 16 (dark mode) and discovered several bugs during human visual review. The big unexpected issue was that the fairness slider quick-commit (044450b, Apr 14 10:05am) had silently gutted MemberEditor.js before phase 16 started — the executor then worked from the broken version. We restored MemberEditor from phase-8 state and added dark classes on top.

The two new memory rules saved: never use Write on existing src/ files (always Edit), and always run vitest + check diff stat after quick tasks.

Phase 16 is essentially done code-wise. Just needs human sign-off on the visual check (plan 16-05) then code review and verification.
</context>

<next_action>
Ask user to continue dark mode visual review in the browser. They should open the app with `npm run dev`, enable dark mode via DevTools or the Settings toggle, and check each view per the 16-05-PLAN.md checklist. When satisfied, type "approved" to trigger SUMMARY.md creation and phase completion steps.
</next_action>

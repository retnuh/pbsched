---
phase: 16-dark-mode-visuals-toggle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/RoundDisplay.js
  - src/views/MatchEditor.js
autonomous: true
requirements:
  - DARK-05
  - DARK-06
must_haves:
  truths:
    - "Round cards, headers, player chips, and sitting-out chips are readable in dark mode"
    - "All three RoundDisplay render functions (renderMain, renderAttendeeManager, renderAlternatives) have dark coverage"
    - "MatchEditor court cards, zone headers, error labels, bench area, and bottom bar are readable in dark mode"
    - "makeEmptySlot() DOM-created nodes have dark border classes"
    - "Empty slot dashed borders are visible on dark backgrounds"
  artifacts:
    - path: src/views/RoundDisplay.js
      provides: "dark: utility classes on all three render function templates"
      contains: "dark:bg-gray-800"
    - path: src/views/MatchEditor.js
      provides: "dark: utility classes in buildHTML template + makeEmptySlot className strings"
      contains: "dark:bg-gray-800"
  key_links:
    - from: "src/views/RoundDisplay.js"
      to: "renderAttendeeManager() function"
      via: "renderAttendeeManager must be found and dark-patched independently of renderMain"
      pattern: "renderAttendeeManager"
    - from: "src/views/MatchEditor.js"
      to: "makeEmptySlot() function"
      via: "DOM node className string (not template literal) must be updated"
      pattern: "makeEmptySlot"
---

<objective>
Add dark: utility classes to RoundDisplay.js (all three render functions) and MatchEditor.js (buildHTML template + makeEmptySlot DOM node creation). These are the two highest-priority views — used at courts on mobile during active sessions.

Purpose: Courts, player chips, bench zones, and round cards must be readable on a dim phone screen in dark mode.
Output: Both view files modified; all cards, chips, labels, error states, and drag zones render correctly in dark mode.
</objective>

<execution_context>
@/Users/hkelly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hkelly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-dark-mode-visuals-toggle/16-CONTEXT.md
@.planning/phases/16-dark-mode-visuals-toggle/16-RESEARCH.md
@.planning/phases/16-dark-mode-visuals-toggle/16-PATTERNS.md
@.planning/phases/16-dark-mode-visuals-toggle/16-UI-SPEC.md

<interfaces>
<!-- Element-to-dark-class mapping from UI-SPEC and PATTERNS, ready for executor. -->

### RoundDisplay.js — full dark coverage map

renderMain() elements:
- Round cards (played): `bg-white border border-gray-100` → add `dark:bg-gray-800 dark:border-gray-700`
- Round header (played): `bg-gray-50` → add `dark:bg-gray-700`
- Round header (unplayed): `bg-blue-50` → add `dark:bg-blue-900/30`
- Round header text (played): `text-gray-500` → add `dark:text-gray-400`
- Round header text (unplayed): `text-blue-800` → add `dark:text-blue-300`
- Completed badge: `text-gray-400 uppercase tracking-widest` → add `dark:text-gray-500`
- Team A cell: `bg-blue-50 border border-blue-100` → add `dark:bg-blue-900/30 dark:border-blue-800`
- Team B cell: `bg-orange-50 border border-orange-100` → add `dark:bg-orange-900/20 dark:border-orange-800`
- Player name text (explicit): wherever `text-gray-900` or `text-gray-700` appears on player name spans → add `dark:text-gray-100` or `dark:text-gray-300`
- Sitting-out label: `text-gray-400 uppercase` → add `dark:text-gray-500`
- Sitting-out chips: `bg-gray-100 text-gray-600 border border-gray-200` → add `dark:bg-gray-700 dark:text-gray-300 dark:border-gray-600`
- Empty state card: `bg-white border border-gray-100` or `border-gray-200` → add `dark:bg-gray-800 dark:border-gray-700`
- Empty state text: `text-gray-500` → add `dark:text-gray-400`
- Sticky footer bar: `bg-white/90 border-t border-gray-100` → add `dark:bg-gray-800/90 dark:border-gray-700`

renderAttendeeManager() elements (MUST be audited separately — this is Pitfall 4):
- Attendee row labels: `bg-white rounded-xl border ${isAttending ? 'border-blue-500 bg-blue-50' : 'border-gray-100'}` → add `dark:bg-gray-800` to base + dark variants in both ternary branches: attending=`dark:bg-blue-900/30`, not-attending=`dark:border-gray-700`
- Add-member form: `bg-blue-50 border border-blue-100` → add `dark:bg-blue-900/20 dark:border-blue-800`
- Sit-count sub-text: `text-gray-400 uppercase` → add `dark:text-gray-500`
- Input in add-member form: `border-gray-200` → add `dark:bg-gray-700 dark:border-gray-600 dark:text-gray-100`

renderAlternatives() elements (MUST be audited separately):
- Alternative cards: `bg-white border-gray-100` → add `dark:bg-gray-800 dark:border-gray-700`
- Alternative header: `bg-gray-50` → add `dark:bg-gray-700`
- Inline team cells: same Team A/B patterns as renderMain

### MatchEditor.js — full dark coverage map

buildHTML() template literals:
- Court card container (data-court div): `bg-white border border-gray-200` → add `dark:bg-gray-800 dark:border-gray-700`
- Court header (bg-gray-50): add `dark:bg-gray-700`
- Court label text (text-gray-500 uppercase): add `dark:text-gray-400`
- Error label (data-court-error, text-red-600): add `dark:text-red-400`
- Team B zone divider (border-l border-gray-200): add `dark:border-gray-700`
- emptySlotHTML const (border-dashed border-gray-200): add `dark:border-gray-600`
- Bench area container (bg-gray-100 border-gray-200): add `dark:bg-gray-700 dark:border-gray-600`
- Bench label (text-gray-500 uppercase): add `dark:text-gray-400`
- Bottom action bar (bg-white/90 border-t border-gray-100): add `dark:bg-gray-800/90 dark:border-gray-700`
- Add court button (bg-blue-50 text-blue-600 border-blue-100): add `dark:bg-blue-900/30 dark:text-blue-300 dark:border-blue-800`
- Remove court button (text-gray-400): add `dark:text-gray-500`

makeEmptySlot() function (MUST update div.className strings directly — not template literals):
- Bench empty slot: `'empty-slot min-h-[44px] px-6 border-2 border-dashed border-gray-200 rounded-full flex items-center justify-center text-gray-300 text-lg'`
  → append ` dark:border-gray-600 dark:text-gray-600`
- Court empty slot: `'empty-slot min-h-[44px] border-2 border-dashed border-gray-200 rounded-full'`
  → append ` dark:border-gray-600`

Sit-out count badge (if present in MatchEditor renderZone or buildHTML):
- `bg-gray-200 text-gray-600` → add `dark:bg-gray-600 dark:text-gray-200`

Toast (showToast): bg-gray-900 text-white — DO NOT CHANGE (already dark-safe per CONTEXT.md Decision 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark utility classes to all three render functions in RoundDisplay.js</name>
  <files>src/views/RoundDisplay.js</files>
  <read_first>
    - src/views/RoundDisplay.js (read the FULL file — all three render functions must be identified: renderMain, renderAttendeeManager, renderAlternatives. Do not assume function names or line numbers.)
  </read_first>
  <action>
    Read src/views/RoundDisplay.js in full. Identify the three render functions. Apply dark: classes to each independently.

    Work through the file systematically top-to-bottom, applying the dark override map from the interfaces section above. Key rules:

    1. NEVER modify the logic — only add `dark:*` classes to existing class strings
    2. In ternary expressions that conditionally add classes (e.g., `isAttending ? 'border-blue-500 bg-blue-50' : 'border-gray-100'`), add dark variants to BOTH branches of the ternary
    3. For `renderAttendeeManager()`: find it explicitly and audit it fully — it is a separate function, not called by renderMain
    4. For `renderAlternatives()`: find it explicitly and audit it fully — same requirement

    Specific ternary pattern for attendee rows (renderAttendeeManager):
    ```javascript
    // BEFORE:
    `border ${isAttending ? 'border-blue-500 bg-blue-50' : 'border-gray-100'}`
    // AFTER:
    `border ${isAttending ? 'border-blue-500 bg-blue-50 dark:bg-blue-900/30' : 'border-gray-100 dark:border-gray-700'} dark:bg-gray-800`
    // Note: dark:bg-gray-800 goes outside the ternary on the base element
    ```

    Specific pattern for sticky footer bar:
    - Find `bg-white/90` on a sticky footer element → add `dark:bg-gray-800/90`
    - Find `border-t border-gray-100` → add `dark:border-gray-700`

    After completing all dark overrides, verify the file still parses as valid JS by checking that all template literals are still properly closed. Do not break any existing functionality — only add classes.
  </action>
  <verify>
    <automated>grep -c "dark:bg-gray-800" /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js</automated>
  </verify>
  <acceptance_criteria>
    - RoundDisplay.js contains `dark:bg-gray-800` (card containers, attendee rows, empty state cards)
    - RoundDisplay.js contains `dark:bg-gray-700` (played round header background)
    - RoundDisplay.js contains `dark:bg-blue-900/30` (unplayed round header, Team A cell, attending attendee row)
    - RoundDisplay.js contains `dark:bg-orange-900/20` (Team B cell)
    - RoundDisplay.js contains `dark:border-gray-700` (card borders, footer border)
    - RoundDisplay.js contains `dark:text-gray-400` (hint text, completed badge area)
    - RoundDisplay.js contains `dark:bg-gray-700 dark:text-gray-300 dark:border-gray-600` (sitting-out chips)
    - RoundDisplay.js contains `dark:bg-gray-800/90` (sticky footer)
    - Verify: `grep "renderAttendeeManager" src/views/RoundDisplay.js` — confirm function exists and was found; `grep "dark:" src/views/RoundDisplay.js | wc -l` — should be > 15 occurrences
    - `npx vitest run src/views/RoundDisplay.test.js` passes (if test file exists)
  </acceptance_criteria>
  <done>All three render functions in RoundDisplay.js have dark: overrides; switching .dark on html in DevTools makes all round cards, attendee rows, alternatives, and sitting-out chips readable on dark background.</done>
</task>

<task type="auto">
  <name>Task 2: Add dark utility classes to MatchEditor.js buildHTML template and makeEmptySlot DOM creation</name>
  <files>src/views/MatchEditor.js</files>
  <read_first>
    - src/views/MatchEditor.js (read the FULL file — specifically find: buildHTML function (template literals), makeEmptySlot function (DOM node className), showToast function (leave unchanged))
  </read_first>
  <action>
    Read src/views/MatchEditor.js in full. Apply dark: classes in two distinct areas:

    ## Area 1: buildHTML() template literals

    Apply the dark override map from the interfaces section:

    - Court card container (`data-court` div with `bg-white border border-gray-200`): add `dark:bg-gray-800 dark:border-gray-700`
    - Court header (`bg-gray-50`): add `dark:bg-gray-700`
    - Court label text (`text-gray-500 uppercase`): add `dark:text-gray-400`
    - Error label span (`data-court-error`, `text-red-600`): add `dark:text-red-400`
    - Team B zone divider (`border-l border-gray-200`): add `dark:border-gray-700`
    - `emptySlotHTML` constant (the `border-dashed border-gray-200` div): add `dark:border-gray-600`
    - Bench area container (`bg-gray-100 border border-gray-200`): add `dark:bg-gray-700 dark:border-gray-600`
    - Bench label (`text-gray-500 uppercase`): add `dark:text-gray-400`
    - Bottom action bar (`bg-white/90 border-t border-gray-100`): add `dark:bg-gray-800/90 dark:border-gray-700`
    - Add court button (`bg-blue-50 text-blue-600 border border-blue-100`): add `dark:bg-blue-900/30 dark:text-blue-300 dark:border-blue-800`
    - Remove court button (`text-gray-400`): add `dark:text-gray-500`

    If a sit-out count badge exists (likely `bg-gray-200 text-gray-600` on the bench):
    - Add `dark:bg-gray-600 dark:text-gray-200`

    ## Area 2: makeEmptySlot() function

    This function creates DOM nodes via `div.className = '...'` — NOT via template literals. Find both className assignments and update them:

    Bench empty slot className:
    ```javascript
    // BEFORE:
    div.className = 'empty-slot min-h-[44px] px-6 border-2 border-dashed border-gray-200 rounded-full flex items-center justify-center text-gray-300 text-lg';
    // AFTER:
    div.className = 'empty-slot min-h-[44px] px-6 border-2 border-dashed border-gray-200 dark:border-gray-600 rounded-full flex items-center justify-center text-gray-300 dark:text-gray-600 text-lg';
    ```

    Court empty slot className:
    ```javascript
    // BEFORE:
    div.className = 'empty-slot min-h-[44px] border-2 border-dashed border-gray-200 rounded-full';
    // AFTER:
    div.className = 'empty-slot min-h-[44px] border-2 border-dashed border-gray-200 dark:border-gray-600 rounded-full';
    ```

    ## DO NOT CHANGE:
    - showToast(): `bg-gray-900 text-white` — already dark-safe (CONTEXT.md Decision 5)
    - validateAndUpdateUI(): `border-red-400` toggle and `border-gray-200` toggle stay as-is; the dark:border-gray-700 added to the court card static HTML covers the dark state via cascade
  </action>
  <verify>
    <automated>grep -c "dark:bg-gray-800\|dark:bg-gray-700\|dark:border-gray-600" /Users/hkelly/Dropbox/projects/pbsched/src/views/MatchEditor.js</automated>
  </verify>
  <acceptance_criteria>
    - MatchEditor.js contains `dark:bg-gray-800 dark:border-gray-700` on the court card container
    - MatchEditor.js contains `dark:bg-gray-700` on the court header
    - MatchEditor.js contains `dark:text-red-400` on the error label span
    - MatchEditor.js contains `dark:bg-gray-800/90 dark:border-gray-700` on the bottom action bar
    - MatchEditor.js contains `dark:bg-blue-900/30 dark:text-blue-300 dark:border-blue-800` on the add court button
    - MatchEditor.js contains `dark:border-gray-600` in the makeEmptySlot className strings (both bench and court variants)
    - showToast function still contains `bg-gray-900 text-white` (unchanged)
    - Verify: `grep "dark:border-gray-600" src/views/MatchEditor.js | wc -l` — at least 3 occurrences (emptySlotHTML const + 2 in makeEmptySlot)
    - `npx vitest run src/views/MatchEditor.test.js` passes (if test file exists)
  </acceptance_criteria>
  <done>MatchEditor.js has dark: classes on all court cards, headers, labels, bench, bottom bar; makeEmptySlot creates DOM nodes with dark border classes; dragging and dropping renders correctly on dark background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View JS → DOM | These views produce innerHTML — no new trust boundaries introduced by adding dark: class strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | Template literals in views | accept | Only adding class strings; no logic changes; no new data flows |
</threat_model>

<verification>
After plan execution:
```bash
# RoundDisplay: check dark class count
grep "dark:" src/views/RoundDisplay.js | wc -l
# Expected: > 15

# MatchEditor: check dark border in makeEmptySlot
grep "dark:border-gray-600" src/views/MatchEditor.js
# Expected: at least 3 lines

# Run existing tests (must still pass)
npx vitest run src/views/RoundDisplay.test.js src/views/MatchEditor.test.js
# Expected: all pass
```
</verification>

<success_criteria>
- RoundDisplay.js: all three render functions have dark: coverage; > 15 dark: occurrences total
- MatchEditor.js: buildHTML template literals have dark: coverage; makeEmptySlot className strings have dark:border-gray-600
- Existing test suites for both views pass without modification
- Switching .dark on html in DevTools renders both views fully readable with no white elements visible
</success_criteria>

<output>
After completion, create `.planning/phases/16-dark-mode-visuals-toggle/16-02-SUMMARY.md`
</output>

---
phase: 18-documentation-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/Help.js
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "The Help screen covers all five workflow sections in the correct order"
    - "Match editing is described briefly in the Fixing Things section"
    - "Dark mode is mentioned contextually in Settings & Preferences"
    - "The How It Works section uses plain language with no technical terms"
    - "The footer still shows the app version"
    - "All new HTML uses correct dark mode class pairs"
  artifacts:
    - path: "src/views/Help.js"
      provides: "Full workflow-based Help screen rewrite"
      contains: "Before You Start"
  key_links:
    - from: "src/views/Help.js"
      to: "__APP_VERSION__"
      via: "Vite global inject in footer template literal"
      pattern: "__APP_VERSION__"
---

<objective>
Rewrite the in-app Help screen from its current 4-section feature-based structure to a 5-section workflow-based structure that follows the organizer's journey at a court.

Purpose: A non-developer organizer reading Help.js on their phone should be able to understand what the app does and how to run a session — including the match editor and dark mode — without encountering any technical language.
Output: Updated `src/views/Help.js` with a full replacement of the `el.innerHTML` template literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-documentation-copy/18-CONTEXT.md
@.planning/phases/18-documentation-copy/18-PATTERNS.md

<interfaces>
<!-- Current Help.js structure — executor MUST read the file before editing -->
<!-- Key structural elements to preserve:
  - mount(el, params) function signature
  - export function unmount() {} no-op
  - el.innerHTML = `...` single template literal assignment
  - Outer wrapper: <div class="p-4 space-y-8 pb-32">
  - Back button with window.history.back() onclick
  - Section header pattern: <h2> with numbered badge <span>
  - Content card pattern: bg-white dark:bg-gray-800 p-4 rounded-xl ...
  - Footer: ${__APP_VERSION__} Vite global — keep as-is
-->

Section header pattern (reuse exactly, increment badge number):
```html
<h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
  <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">N</span>
  Section Title
</h2>
```

Content card pattern (reuse exactly):
```html
<div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
  <p>...</p>
</div>
```

Bulleted list pattern:
```html
<ul class="list-disc ml-5 space-y-2">
  <li><strong>Label:</strong> Text.</li>
</ul>
```

Footer pattern (keep exactly):
```html
<footer class="text-center pt-4">
  <p class="text-xs text-gray-400 dark:text-gray-500">Pickleball Practice Scheduler ${__APP_VERSION__}</p>
</footer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Help.js el.innerHTML with workflow-based sections</name>
  <files>src/views/Help.js</files>
  <read_first>
    - src/views/Help.js — read current file in full before making any edits; understand exact structure being replaced
    - .planning/phases/18-documentation-copy/18-CONTEXT.md — decisions D-01 through D-07, specifics block
    - .planning/phases/18-documentation-copy/18-PATTERNS.md — section header, card, list, and footer HTML patterns
  </read_first>
  <action>
Replace the entire `el.innerHTML = \`...\`` template literal (lines 2–66) with the new workflow-based content below. Use the Edit tool (surgical diff) — NEVER the Write tool on this existing source file.

The function signature `export function mount(el, params) {` and the closing `export function unmount() {}` must remain unchanged. Only the template literal content changes.

New el.innerHTML content:

```
el.innerHTML = `
    <div class="p-4 space-y-8 pb-32">
      <header class="flex items-center space-x-4">
        <button onclick="window.history.back()" class="p-2 -ml-2 text-blue-600 dark:text-blue-400">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
        </button>
        <h1 class="text-2xl font-bold">Help & Guide</h1>
      </header>

      <section class="space-y-4">
        <h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
          <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">1</span>
          Before You Start
        </h2>
        <div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
          <p><strong>Create a club:</strong> Tap the club icon and add your club's name. Then add your regular players — you only need to do this once.</p>
          <p><strong>Check in who showed up:</strong> When you start a session, pick the players who are present today. The app puts your most frequent players at the top so check-in is fast.</p>
          <p>You need at least 4 players to generate rounds.</p>
        </div>
      </section>

      <section class="space-y-4">
        <h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
          <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">2</span>
          Running a Session
        </h2>
        <div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
          <p><strong>Generate a round:</strong> Tap "Generate Round" and the app instantly proposes court assignments. Call out the matchups to your group.</p>
          <p><strong>Mark it played:</strong> Once the round is finished, tap "Mark Played." This locks the result into history and queues up the next round.</p>
          <p><strong>Odd number of players?</strong> When your group doesn't divide evenly into 4-person courts, choose a fallback strategy:</p>
          <ul class="list-disc ml-5 space-y-2">
            <li><strong>Play 2v1:</strong> Creates one 3-player court so everyone is playing.</li>
            <li><strong>Play 1v1:</strong> Creates a singles court; any remaining player sits out.</li>
            <li><strong>All Sit:</strong> Standard 2v2 only; extra players sit out this round.</li>
          </ul>
        </div>
      </section>

      <section class="space-y-4">
        <h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
          <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">3</span>
          Fixing Things
        </h2>
        <div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
          <p><strong>Don't like the matchup?</strong> Tap "Alternatives" to see other options for that round, then tap "Select" on the one you prefer.</p>
          <p><strong>Need to swap players?</strong> Tap "Edit" on any unplayed round. Drag players between courts or to the bench, then tap "Confirm" to save your changes.</p>
          <p><strong>Change who sits out:</strong> Tap the sitting-out area on an unplayed round to pick a specific player. The courts will regenerate around your choice.</p>
          <p><strong>Marked played by mistake?</strong> Tap "Undo" on the most recent completed round to reverse it.</p>
        </div>
      </section>

      <section class="space-y-4">
        <h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
          <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">4</span>
          Settings & Preferences
        </h2>
        <div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
          <p><strong>Appearance:</strong> Choose light mode, dark mode, or follow your device's system setting. Find this at the top of Settings.</p>
          <p><strong>Fairness sliders:</strong> Under "Scheduling Preferences" you can adjust how strongly the app avoids repeating the same partners, opponents, or sit-out players. The defaults work well for most groups.</p>
          <p><strong>Backup & Restore:</strong> Share your club data to keep a backup or move to a new device — all from the Settings screen.</p>
        </div>
      </section>

      <section class="space-y-4">
        <h2 class="text-lg font-bold text-blue-600 dark:text-blue-400 flex items-center">
          <span class="w-6 h-6 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full flex items-center justify-center text-xs mr-2">5</span>
          How It Works
        </h2>
        <div class="bg-white dark:bg-gray-800 p-4 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm space-y-3 text-sm leading-relaxed dark:text-gray-300">
          <p>The app runs hundreds of simulations and picks the matchup where everyone plays with the most different people across the session. It tracks who has played with or against each other and who has sat out, so the best mix rises to the top automatically.</p>
        </div>
      </section>

      <footer class="text-center pt-4">
        <p class="text-xs text-gray-400 dark:text-gray-500">Pickleball Practice Scheduler ${__APP_VERSION__}</p>
      </footer>
    </div>
  `;
```

Key constraints:
- Use the Edit tool, not Write — this is an existing file (per CLAUDE.md)
- The `${__APP_VERSION__}` interpolation in the footer is a Vite global — keep the exact string; do not escape the `${}` in the Edit diff
- Do not touch the `export function mount` or `export function unmount` lines
- Every content card div must include `dark:text-gray-300` on the card container (not individual `<p>` tags)
  </action>
  <verify>
    <automated>grep -n "Before You Start" /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js && grep -n "How It Works" /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js && grep -n "__APP_VERSION__" /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js && grep -n "tap .Edit." /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js && grep -n "dark mode\|Dark mode" /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js && ! grep -ni "penalty\|algorithm\|optimization" /Users/hkelly/Dropbox/projects/pbsched/src/views/Help.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Before You Start" src/views/Help.js` returns a match
    - `grep "Running a Session" src/views/Help.js` returns a match
    - `grep "Fixing Things" src/views/Help.js` returns a match
    - `grep "Settings &amp; Preferences\|Settings & Preferences" src/views/Help.js` returns a match
    - `grep "How It Works" src/views/Help.js` returns a match
    - `grep "tap .Edit. on any unplayed round" src/views/Help.js` returns a match (match editing coverage per D-06)
    - `grep -i "dark mode\|light mode" src/views/Help.js` returns a match (dark mode mention per D-07)
    - `grep "__APP_VERSION__" src/views/Help.js` returns a match (footer preserved)
    - `grep -i "penalty\|algorithm\|optimization" src/views/Help.js` returns NO matches (no technical terms per D-05)
    - `grep "window.history.back()" src/views/Help.js` returns a match (back button preserved)
    - File has exactly 5 numbered badge spans: `grep 'justify-center text-xs mr-2">[1-5]<' src/views/Help.js` returns 5 matches
  </acceptance_criteria>
  <done>Help.js contains 5 workflow-based sections (Before You Start, Running a Session, Fixing Things, Settings & Preferences, How It Works), describes match editing and dark mode contextually, contains no technical jargon, and preserves the back button and version footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only static HTML template strings in a view file. No data crosses trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Help.js static content | accept | Documentation only — no PII, no secrets, no auth. Content is intentionally public. |
</threat_model>

<verification>
After the task completes, verify the file builds without errors:

```bash
cd /Users/hkelly/Dropbox/projects/pbsched && npm run build 2>&1 | tail -5
```

Expected: build exits 0. If it fails with a template literal syntax error, the `${__APP_VERSION__}` injection may have been escaped — check the footer line and restore the raw `${__APP_VERSION__}` string.
</verification>

<success_criteria>
1. `src/views/Help.js` has 5 sections with the workflow-based titles specified in D-03 plus "How It Works"
2. No occurrence of "penalty", "algorithm", or "Optimization Settings" in the file
3. Match editing (D-06) and dark mode (D-07) are covered in their respective sections
4. `${__APP_VERSION__}` footer is intact
5. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-copy/18-01-SUMMARY.md` following the summary template.
</output>

---
phase: 18-documentation-copy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "README opens with a single plain sentence about what the app does"
    - "App URL link appears near the top"
    - "Organizer is directed to in-app Help for instructions"
    - "Developer content (Tech Stack, Development, Credits, License) is still present but below a horizontal rule"
    - "No step-by-step instructions exist in the README itself"
    - "The deployment badge is present (moved below the rule with developer content)"
  artifacts:
    - path: "README.md"
      provides: "Organizer-first README with developer content below the rule"
      contains: "Pickleball Practice Scheduler generates"
  key_links:
    - from: "README.md organizer section"
      to: "in-app Help screen"
      via: "plain text pointer"
      pattern: "Help"
---

<objective>
Restructure README.md so it opens with organizer-appropriate content (one plain sentence + app link + Help pointer) and moves all developer content below a horizontal rule.

Purpose: When an organizer finds the app's GitHub page, the first thing they see should be what the app does in plain language — not developer tooling details.
Output: Updated `README.md` with organizer content at the top and developer content below `---`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-documentation-copy/18-CONTEXT.md
@.planning/phases/18-documentation-copy/18-PATTERNS.md

<interfaces>
<!-- Current README.md (56 lines) to be restructured:
  Line 1:  # Pickleball Practice Scheduler (pbsched)
  Line 3:  Badge (deploy workflow)
  Line 5:  Developer-oriented paragraph opener
  Line 7:  Launch link
  Lines 9-16:  ## Features (emoji bullets — REMOVE from organizer section)
  Lines 18-24: ## Tech Stack (KEEP, move below ---)
  Lines 26-40: ## Development (KEEP, move below ---)
  Lines 42-49: ## Credits (KEEP, move below ---)
  Lines 51-52: ## License (KEEP, move below ---)
  Lines 54-55: --- and footer tagline
-->

<!-- Target shape per decisions D-08, D-09, D-10:
  # Pickleball Practice Scheduler

  [one plain sentence — no emoji, no jargon]

  **[Launch the app](https://retnuh.github.io/pbsched/)**

  [one short note directing to in-app Help — no step-by-step list]

  ---

  [badge]

  ## Tech Stack ... (existing content, kept verbatim)
  ## Development ... (existing content, kept verbatim)
  ## Credits ... (existing content, kept verbatim)
  ## License ... (existing content, kept verbatim)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README.md — organizer content first, developer content below rule</name>
  <files>README.md</files>
  <read_first>
    - README.md — read current file in full before making any edits; understand exact current content
    - .planning/phases/18-documentation-copy/18-CONTEXT.md — decisions D-08, D-09, D-10 and the specifics block (plain sentence direction)
    - .planning/phases/18-documentation-copy/18-PATTERNS.md — README restructure section
  </read_first>
  <action>
Replace the entire README.md content using the Edit tool (never the Write tool on an existing file — per CLAUDE.md).

The new README.md content should be:

```markdown
# Pickleball Practice Scheduler

Pickleball Practice Scheduler generates fair, varied round matchups for practice sessions — so you can focus on the game, not the math.

**[Launch the app](https://retnuh.github.io/pbsched/)**

Open the app and tap **Help** for full instructions on setting up your club and running a session.

---

[![Deploy static content to Pages](https://github.com/retnuh/pbsched/actions/workflows/deploy.yml/badge.svg)](https://github.com/retnuh/pbsched/actions/workflows/deploy.yml)

## Tech Stack

- **Framework:** Vanilla JavaScript (ES Modules)
- **Bundler:** [Vite 8](https://vitejs.dev/)
- **Styling:** [Tailwind CSS 4](https://tailwindcss.com/)
- **Testing:** [Vitest](https://vitest.dev/)
- **PWA:** Custom Service Worker implementation for offline caching.

## Development

```bash
# Install dependencies
just install

# Start development server
just dev

# Run tests
just test

# Build and Deploy
just deploy
```

## Credits

This project was built with a collaborative AI workflow:

- **[GSD (Get Shit Done)](https://github.com/retnuh/gsd):** The foundational methodology and system prompts that orchestrated the development.
- **[Google Gemini](https://gemini.google.com/):** For high-speed context handling, architectural decisions, and the heavy lifting of the initial implementation.
- **[Anthropic Claude](https://claude.ai/):** For the initial project research, UX brainstorming, and high-fidelity project specification.

## License

This project is released into the public domain under the [Unlicense](./UNLICENSE). Feel free to use, modify, and distribute it however you like.
```

Key constraints:
- Use the Edit tool, not Write (this is an existing file)
- The organizer section must not contain step-by-step instructions (D-10)
- The Features section (emoji bullet list) is REMOVED — replaced by the plain sentence opener
- The deployment badge moves below the `---` rule (it belongs with developer content)
- Tech Stack, Development commands (fenced code block), Credits, and License are kept verbatim after the rule — do not paraphrase or condense them
- The old developer-oriented opener paragraph ("A lightweight, mobile-first Progressive Web App...") is replaced by the plain sentence
- The old footer tagline ("Built for the love of the game...") is dropped — it was below the rule anyway and is optional content
  </action>
  <verify>
    <automated>head -5 /Users/hkelly/Dropbox/projects/pbsched/README.md && grep -n "generates fair" /Users/hkelly/Dropbox/projects/pbsched/README.md && grep -n "^---$" /Users/hkelly/Dropbox/projects/pbsched/README.md && grep -n "Tech Stack" /Users/hkelly/Dropbox/projects/pbsched/README.md && grep -n "Credits" /Users/hkelly/Dropbox/projects/pbsched/README.md && ! grep -n "optimization algorithm\|brain-melting\|PWA" /Users/hkelly/Dropbox/projects/pbsched/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -3 README.md` shows the title line then the plain sentence (no badge, no developer paragraph at the top)
    - `grep "generates fair, varied round matchups" README.md` returns a match on line 3 or 4
    - `grep "retnuh.github.io/pbsched" README.md` returns a match (app URL present)
    - `grep -i "help" README.md` returns a match pointing organizers to in-app Help
    - `grep "^---$" README.md` returns exactly one match (the horizontal rule separating sections)
    - `grep "Tech Stack" README.md` returns a match (developer section preserved)
    - `grep "just install" README.md` returns a match (dev commands preserved)
    - `grep "Credits" README.md` returns a match (credits preserved)
    - `grep "Unlicense" README.md` returns a match (license preserved)
    - `grep "deploy.yml/badge.svg" README.md` returns a match (badge preserved)
    - `grep "optimization algorithm\|brain-melting" README.md` returns NO matches (old developer opener removed)
    - `grep "^## ✨ Features" README.md` returns NO matches (emoji Features section removed)
    - `grep "step-by-step\|Step 1\|Step 2" README.md` returns NO matches (no instructions in README per D-10)
  </acceptance_criteria>
  <done>README.md opens with one plain sentence describing the app, followed by the launch link and a pointer to in-app Help; all developer content (badge, Tech Stack, Development, Credits, License) is present and unchanged below the horizontal rule separator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only a Markdown documentation file. No data crosses trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Information Disclosure | README.md | accept | Public documentation — content is intentionally public. No PII, no secrets. |
</threat_model>

<verification>
After the task completes, verify:

```bash
head -10 /Users/hkelly/Dropbox/projects/pbsched/README.md
```

Expected: Line 1 is the `# Pickleball Practice Scheduler` heading, line 3 is the plain sentence starting with "Pickleball Practice Scheduler generates", line 5 is the launch link. No badge on line 3.
</verification>

<success_criteria>
1. README.md line 1 is the title, line 3 is the plain organizer sentence
2. A single `---` rule separates organizer content from developer content
3. Tech Stack, Development commands, Credits, and License are all present below the rule
4. No `## ✨ Features` section exists
5. No step-by-step session instructions appear in the file
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-copy/18-02-SUMMARY.md` following the summary template.
</output>

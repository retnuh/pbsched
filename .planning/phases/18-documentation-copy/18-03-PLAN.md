---
phase: 18-documentation-copy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/RoundDisplay.js
  - src/views/Settings.js
  - src/views/SessionSetup.js
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "Alternative matchup options show quality labels instead of raw scores"
    - "Settings heading no longer says Scheduler Optimization"
    - "Settings subheading no longer says Short-Sided Matches"
    - "Settings labels 3-Way Solo and 3-Way Pair are replaced with plain language"
    - "The empty-rounds fallback message in RoundDisplay is clearer"
    - "No other view has developer-jargon copy that needs changing"
  artifacts:
    - path: "src/views/RoundDisplay.js"
      provides: "Quality label for alternative matchup options"
      contains: "Best Match"
    - path: "src/views/Settings.js"
      provides: "Plain-language scheduler section labels"
      contains: "Scheduling Preferences"
  key_links:
    - from: "src/views/RoundDisplay.js renderAlternatives()"
      to: "alternatives panel h3 label"
      via: "template literal at line ~174"
      pattern: "Option.*Best Match\\|Best Match"
---

<objective>
Audit and fix user-facing copy across all views. Make four surgical edits: quality labels in RoundDisplay alternatives panel, heading/label replacements in Settings, a clearer empty-state fallback in RoundDisplay, and a discretionary label improvement in SessionSetup. All other views were audited in 18-PATTERNS.md and require no changes.

Purpose: An organizer reading any screen should not encounter developer-internal language or raw algorithmic output (like a raw score number).
Output: Surgical text edits to `src/views/RoundDisplay.js`, `src/views/Settings.js`, and `src/views/SessionSetup.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-documentation-copy/18-CONTEXT.md
@.planning/phases/18-documentation-copy/18-PATTERNS.md

<interfaces>
<!-- RoundDisplay.js — renderAlternatives() at line ~174:
CURRENT:
  `Option ${index + 1} (Score: ${Math.round(alt.score)})`

TARGET (per D-12, quality label pattern):
  index 0 → "Best Match"
  index 1 → "Good Match"
  index 2+ → "Option ${index + 1}"
Use a ternary or helper expression inside the template literal.
Suggested inline expression:
  ${index === 0 ? 'Best Match' : index === 1 ? 'Good Match' : `Option ${index + 1}`}
-->

<!-- RoundDisplay.js — empty-rounds state at line ~299:
CURRENT:
  "Wait, where did the rounds go?"
TARGET:
  "No rounds yet — tap Generate Round to get started."
-->

<!-- Settings.js surgical edits — 4 text changes:
1. Line ~33: "Scheduler Optimization" → "Scheduling Preferences"
2. Line ~65: "Short-Sided Matches" → "Uneven Courts"
3. Line ~80: label text "3-Way Solo" → "Solo on 3-Player Court"
4. Line ~92: label text "3-Way Pair" → "Pair on 3-Player Court"
Each label is inside a <label> element within the .innerHTML template literal.
-->

<!-- SessionSetup.js — discretionary label:
Line ~66: button/label "Invert" → "Invert Selection"
This is a small text-only change to a button label.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoundDisplay.js — quality labels + empty-state message</name>
  <files>src/views/RoundDisplay.js</files>
  <read_first>
    - src/views/RoundDisplay.js — read lines 160–310 to confirm exact current strings before editing
    - .planning/phases/18-documentation-copy/18-PATTERNS.md — RoundDisplay.js section (lines ~95–126)
    - .planning/phases/18-documentation-copy/18-CONTEXT.md — decisions D-11, D-12
  </read_first>
  <action>
Make exactly two surgical text edits to `src/views/RoundDisplay.js` using the Edit tool (never the Write tool — this is an existing file per CLAUDE.md).

**Edit 1 — Alternative option quality labels (line ~174 in renderAlternatives()):**

Find this exact string:
```
`Option ${index + 1} (Score: ${Math.round(alt.score)})`
```

Replace with:
```
${index === 0 ? 'Best Match' : index === 1 ? 'Good Match' : `Option ${index + 1}`}
```

The full `<h3>` line context around the target (for locating it precisely):
```html
<h3 class="font-bold text-gray-500 dark:text-gray-400 uppercase tracking-widest text-xs">Option ${index + 1} (Score: ${Math.round(alt.score)})</h3>
```

After the edit it should read:
```html
<h3 class="font-bold text-gray-500 dark:text-gray-400 uppercase tracking-widest text-xs">${index === 0 ? 'Best Match' : index === 1 ? 'Good Match' : `Option ${index + 1}`}</h3>
```

Note: This expression is inside a nested template literal (the outer `.map()` callback is itself inside `el.innerHTML = \`...\``). The inner backtick in `` `Option ${index + 1}` `` must be escaped as `\`Option ${index + 1}\`` if the outer context requires it — check the surrounding code carefully when reading the file and match the existing escaping convention.

**Edit 2 — Empty-rounds fallback message (line ~299):**

Find this exact string:
```
Wait, where did the rounds go?
```

Replace with:
```
No rounds yet — tap Generate Round to get started.
```

The full `<p>` line context:
```html
<p class="text-gray-500 dark:text-gray-400">Wait, where did the rounds go?</p>
```

After the edit:
```html
<p class="text-gray-500 dark:text-gray-400">No rounds yet — tap Generate Round to get started.</p>
```
  </action>
  <verify>
    <automated>grep -n "Best Match" /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js && grep -n "Good Match" /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js && grep -n "No rounds yet" /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js && ! grep -n "Score: " /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js && ! grep -n "where did the rounds go" /Users/hkelly/Dropbox/projects/pbsched/src/views/RoundDisplay.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Best Match" src/views/RoundDisplay.js` returns a match (index 0 label)
    - `grep "Good Match" src/views/RoundDisplay.js` returns a match (index 1 label)
    - `grep "Score: " src/views/RoundDisplay.js` returns NO matches (raw score removed)
    - `grep "Math.round(alt.score)" src/views/RoundDisplay.js` returns NO matches (score expression removed)
    - `grep "No rounds yet" src/views/RoundDisplay.js` returns a match (new empty state message)
    - `grep "where did the rounds go" src/views/RoundDisplay.js` returns NO matches (old message removed)
    - `cd /Users/hkelly/Dropbox/projects/pbsched && npm run build` exits 0 (template literal syntax valid)
  </acceptance_criteria>
  <done>RoundDisplay.js alternatives panel shows "Best Match" / "Good Match" / "Option N" labels instead of raw scores, and the empty-rounds state shows a clear prompt instead of the informal internal message.</done>
</task>

<task type="auto">
  <name>Task 2: Settings.js + SessionSetup.js — plain-language label replacements</name>
  <files>src/views/Settings.js, src/views/SessionSetup.js</files>
  <read_first>
    - src/views/Settings.js — read lines 25–105 to confirm exact current strings and their surrounding HTML context
    - src/views/SessionSetup.js — read lines 55–75 to confirm the "Invert" button label and its surrounding context
    - .planning/phases/18-documentation-copy/18-PATTERNS.md — Settings.js section (summary table at bottom)
    - .planning/phases/18-documentation-copy/18-CONTEXT.md — decision D-11 (scope of copy audit)
  </read_first>
  <action>
Make surgical text edits to `src/views/Settings.js` and `src/views/SessionSetup.js` using the Edit tool only (never Write on existing source files per CLAUDE.md).

**Settings.js — 4 text-only changes:**

**Change 1 — Section heading (line ~33):**
Find:
```html
<h2 class="font-bold text-gray-700 dark:text-gray-200">Scheduler Optimization</h2>
```
Replace with:
```html
<h2 class="font-bold text-gray-700 dark:text-gray-200">Scheduling Preferences</h2>
```

**Change 2 — Subheading (line ~65):**
Find:
```html
<p class="text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wide mt-2">Short-Sided Matches</p>
```
Replace with:
```html
<p class="text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wide mt-2">Uneven Courts</p>
```

**Change 3 — 3-Way Solo label (line ~80):**
Find:
```html
                <label>3-Way Solo</label>
```
Replace with:
```html
                <label>Solo on 3-Player Court</label>
```

**Change 4 — 3-Way Pair label (line ~92):**
Find:
```html
                <label>3-Way Pair</label>
```
Replace with:
```html
                <label>Pair on 3-Player Court</label>
```

Note: Read the file first to confirm exact whitespace/indentation on the `<label>` lines before applying the diff. Match the indentation exactly.

**SessionSetup.js — 1 text-only change:**

Read the file around line 66 to locate the "Invert" button label. The label text is the only thing changing — the button's classes, id, and all surrounding HTML stay the same.

Find the button text:
```
Invert
```
(inside the button element near the player selection header)

Replace the button's text content with:
```
Invert Selection
```

Only the visible text changes. Do not modify the button's class, id, or any other attribute.
  </action>
  <verify>
    <automated>grep -n "Scheduling Preferences" /Users/hkelly/Dropbox/projects/pbsched/src/views/Settings.js && grep -n "Uneven Courts" /Users/hkelly/Dropbox/projects/pbsched/src/views/Settings.js && grep -n "Solo on 3-Player Court" /Users/hkelly/Dropbox/projects/pbsched/src/views/Settings.js && grep -n "Pair on 3-Player Court" /Users/hkelly/Dropbox/projects/pbsched/src/views/Settings.js && ! grep -n "Scheduler Optimization\|Short-Sided Matches\|3-Way Solo\|3-Way Pair" /Users/hkelly/Dropbox/projects/pbsched/src/views/Settings.js && grep -n "Invert Selection" /Users/hkelly/Dropbox/projects/pbsched/src/views/SessionSetup.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Scheduling Preferences" src/views/Settings.js` returns a match
    - `grep "Uneven Courts" src/views/Settings.js` returns a match
    - `grep "Solo on 3-Player Court" src/views/Settings.js` returns a match
    - `grep "Pair on 3-Player Court" src/views/Settings.js` returns a match
    - `grep "Scheduler Optimization" src/views/Settings.js` returns NO matches
    - `grep "Short-Sided Matches" src/views/Settings.js` returns NO matches
    - `grep "3-Way Solo" src/views/Settings.js` returns NO matches
    - `grep "3-Way Pair" src/views/Settings.js` returns NO matches
    - `grep "Invert Selection" src/views/SessionSetup.js` returns a match
    - `grep -w "^.*>Invert<" src/views/SessionSetup.js` returns NO matches (old label gone)
    - `cd /Users/hkelly/Dropbox/projects/pbsched && npm run build` exits 0
  </acceptance_criteria>
  <done>Settings.js uses plain organizer language for all four scheduler-section labels; SessionSetup.js "Invert" button now reads "Invert Selection".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan makes text-only changes to HTML template strings inside view files. No data crosses trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Information Disclosure | View copy strings | accept | Text changes only — no PII, no auth, no data handling. Strings are static UI copy. |
</threat_model>

<verification>
After both tasks complete, run the full test suite to confirm no regressions from template literal edits:

```bash
cd /Users/hkelly/Dropbox/projects/pbsched && npm test 2>&1 | tail -20
```

Expected: all tests pass. If a test fails referencing the old string "Option N (Score:" or "Wait, where did the rounds go", update the test assertion to match the new copy — those are test-copy regressions, not functional bugs.

Also verify the build:
```bash
cd /Users/hkelly/Dropbox/projects/pbsched && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. RoundDisplay alternatives panel uses quality labels (Best Match / Good Match / Option N) with no raw score numbers
2. RoundDisplay empty-rounds state shows "No rounds yet — tap Generate Round to get started."
3. Settings section heading reads "Scheduling Preferences" (not "Scheduler Optimization")
4. Settings subheading reads "Uneven Courts" (not "Short-Sided Matches")
5. Settings labels read "Solo on 3-Player Court" and "Pair on 3-Player Court" (not "3-Way Solo" / "3-Way Pair")
6. SessionSetup "Invert" button reads "Invert Selection"
7. `npm test` passes
8. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-copy/18-03-SUMMARY.md` following the summary template.
</output>

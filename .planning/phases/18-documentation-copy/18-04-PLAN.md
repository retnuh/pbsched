---
phase: 18-documentation-copy
plan: 04
type: execute
wave: 1
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - src/views/Help.js
  - src/views/RoundDisplay.js
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "Help 'Before You Start' says 2 players minimum, not 4"
    - "Help 'Running a Session' has no 3-bullet strategy list"
    - "Help 'Fixing Things' clarifies that bench means sitting out / resting"
    - "Help 'Fixing Things' has no confusing 'Change who sits out' paragraph"
    - "Help 'Fixing Things' mentions editing the most recently played round"
    - "Help 'Settings & Preferences' notes that relative slider values matter more than absolutes"
    - "Alternatives panel labels all options with quality words; no 'Option N' fallback"
  artifacts:
    - path: src/views/Help.js
      provides: "Updated help text for all 7 gaps in the help screen"
    - path: src/views/RoundDisplay.js
      provides: "Extended quality label sequence with no numbered fallback"
  key_links:
    - from: src/views/Help.js
      to: "user-visible help screen"
      via: "el.innerHTML template string"
      pattern: "at least 2 players"
    - from: src/views/RoundDisplay.js
      to: "alternatives panel header labels"
      via: "renderAlternatives() ternary expression"
      pattern: "Okay Match|Meh|Not Great|Bad"
---

<objective>
Close seven UAT gaps (GAP-01 through GAP-07) identified after Phase 18 plans 01-03 were executed. All changes are surgical text edits — no new UI components, no new logic.

Purpose: Ensure the help screen copy is accurate, clear, and complete; and that the alternatives panel quality labels never fall back to generic "Option N" numbering.
Output: Updated Help.js and RoundDisplay.js with all seven gaps resolved.
</objective>

<execution_context>
@/Users/hkelly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hkelly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-documentation-copy/18-CONTEXT.md
@.planning/phases/18-documentation-copy/18-03-SUMMARY.md

CRITICAL: Use the Edit tool (surgical diff) on BOTH files. NEVER use Write on existing source files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Help.js — GAP-01, GAP-02, GAP-03, GAP-04, GAP-05, GAP-07</name>
  <files>src/views/Help.js</files>

  <read_first>
    src/views/Help.js — read the FULL current file before making any edits. The exact
    strings below are verified against the file as of plan-writing time, but always
    confirm current content matches before applying an Edit diff.
  </read_first>

  <action>
Apply six independent surgical edits to src/views/Help.js using the Edit tool. Each
edit is independent; apply them in order.

---

EDIT 1 — GAP-01: Fix minimum player count (line 19)

Find (exact):
  You need at least 4 players to generate rounds.

Replace with:
  You need at least 2 players to generate rounds.

---

EDIT 2 — GAP-02: Remove odd-player strategy list (lines 31-36)

Find (exact, including the newline before the closing tag):
          <p><strong>Odd number of players?</strong> When your group doesn't divide evenly into 4-person courts, choose a fallback strategy:</p>
          <ul class="list-disc ml-5 space-y-2">
            <li><strong>Play 2v1:</strong> Creates one 3-player court so everyone is playing.</li>
            <li><strong>Play 1v1:</strong> Creates a singles court; any remaining player sits out.</li>
            <li><strong>All Sit:</strong> Standard 2v2 only; extra players sit out this round.</li>
          </ul>

Replace with:
          <p><strong>Odd number of players?</strong> If the round doesn't look right, tap Edit to rearrange players between courts however you like.</p>

---

EDIT 3 — GAP-03: Add bench clarification (line 47)

Find (exact):
  Drag players between courts or to the bench, then tap "Confirm" to save your changes.

Replace with:
  Drag players between courts or to the bench (bench means the player is sitting out / resting that round), then tap "Confirm" to save your changes.

---

EDIT 4 — GAP-04: Remove confusing "Change who sits out" paragraph (line 48)

Find (exact):
          <p><strong>Change who sits out:</strong> Tap the sitting-out area on an unplayed round to pick a specific player. The courts will regenerate around your choice.</p>

Replace with:
  (nothing — delete the line entirely)

Note: after deletion there will be two consecutive `<p>` tags — the "Need to swap players?" paragraph and the "Marked played by mistake?" paragraph. That's correct.

---

EDIT 5 — GAP-05: Add sentence about editing a played round

Find (exact):
          <p><strong>Marked played by mistake?</strong> Tap "Undo" on the most recent completed round to reverse it.</p>

Replace with:
          <p><strong>Need to correct a played round?</strong> Tap "Edit" on the most recently played round to fix it — keeping the history accurate helps the app suggest better matchups going forward.</p>
          <p><strong>Marked played by mistake?</strong> Tap "Undo" on the most recent completed round to reverse it.</p>

---

EDIT 6 — GAP-07: Add relative-values note to fairness sliders paragraph

Find (exact):
  <p><strong>Fairness sliders:</strong> Under "Scheduling Preferences" you can adjust how strongly the app avoids repeating the same partners, opponents, or sit-out players. The defaults work well for most groups.</p>

Replace with:
  <p><strong>Fairness sliders:</strong> Under "Scheduling Preferences" you can adjust how strongly the app avoids repeating the same partners, opponents, or sit-out players. The defaults work well for most groups. The exact numbers aren't critical — what matters is how the sliders compare to each other.</p>
  </action>

  <verify>
    Run all of these grep checks after editing. Each must return a match:

    grep -n "at least 2 players" src/views/Help.js
    # Must find "You need at least 2 players to generate rounds."

    grep -n "tap Edit to rearrange players" src/views/Help.js
    # Must find the replacement odd-player sentence

    grep -n "bench means the player is sitting out" src/views/Help.js
    # Must find the bench clarification

    grep -n "Need to correct a played round" src/views/Help.js
    # Must find the new played-round editing sentence

    grep -n "what matters is how the sliders compare" src/views/Help.js
    # Must find the relative-values note

    And these must return NO matches (lines were removed):

    grep -n "at least 4 players" src/views/Help.js
    # Must return empty

    grep -n "Change who sits out" src/views/Help.js
    # Must return empty

    grep -n "list-disc ml-5" src/views/Help.js
    # Must return empty (strategy list removed)
  </verify>

  <acceptance_criteria>
    - "You need at least 2 players" appears in Before You Start section
    - "You need at least 4 players" does NOT appear anywhere in the file
    - No `<ul class="list-disc` block exists in the file (strategy list gone)
    - "tap Edit to rearrange players" appears in Running a Session
    - "bench means the player is sitting out / resting that round" appears in Fixing Things
    - "Change who sits out" does NOT appear anywhere in the file
    - "Need to correct a played round?" paragraph appears before "Marked played by mistake?"
    - "what matters is how the sliders compare to each other" appears in Settings & Preferences
    - File still exports `mount` and `unmount` functions (structure intact)
  </acceptance_criteria>

  <done>All six text edits applied; grep checks pass; no 4-player mention; no strategy list; no "Change who sits out" paragraph; bench clarification, played-round editing sentence, and slider relative-values note all present.</done>
</task>

<task type="auto">
  <name>Task 2: Fix RoundDisplay.js — GAP-06 quality label sequence</name>
  <files>src/views/RoundDisplay.js</files>

  <read_first>
    src/views/RoundDisplay.js — read the full file, specifically the renderAlternatives()
    function (around line 174) to confirm the exact current ternary expression for the
    quality label before applying the edit.
  </read_first>

  <action>
Apply one surgical edit to src/views/RoundDisplay.js inside the renderAlternatives()
function using the Edit tool.

Find (exact, around line 174):
                <h3 class="font-bold text-gray-500 dark:text-gray-400 uppercase tracking-widest text-xs">${index === 0 ? 'Best Match' : index === 1 ? 'Good Match' : `Option ${index + 1}`}</h3>

Replace with:
                <h3 class="font-bold text-gray-500 dark:text-gray-400 uppercase tracking-widest text-xs">${['Best Match', 'Good Match', 'Okay Match', 'Meh', 'Not Great', 'Bad'][index] ?? 'Bad'}</h3>

Explanation of the replacement:
- Array lookup covers indices 0–5 with human-readable quality labels.
- The nullish coalescing `?? 'Bad'` handles index 6+ (more than 6 alternatives)
  by falling back to 'Bad' rather than an "Option N" string.
- No numbered fallback exists anywhere in the new expression.
  </action>

  <verify>
    grep -n "Okay Match" src/views/RoundDisplay.js
    # Must find the new array expression

    grep -n "Option \${index" src/views/RoundDisplay.js
    # Must return empty (old numbered fallback gone)

    grep -n "Option \${" src/views/RoundDisplay.js
    # Must return empty
  </verify>

  <acceptance_criteria>
    - The string `'Okay Match'` appears in renderAlternatives() label expression
    - The string `'Meh'` appears in renderAlternatives() label expression
    - The string `'Not Great'` appears in renderAlternatives() label expression
    - The string `'Bad'` appears in renderAlternatives() label expression
    - The pattern `` `Option ${index + 1}` `` does NOT appear anywhere in the file
    - The expression uses array index lookup (`['Best Match', 'Good Match', ...]`) not a chain of ternaries
    - File still exports `mount` and `unmount` functions (structure intact)
  </acceptance_criteria>

  <done>Quality label array covers indices 0–5 plus a 'Bad' fallback for index 6+; no "Option N" pattern remains in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static help text → user | All changes are static strings with no user input — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering | Help.js static strings | accept | Read-only display text; no user input or data processing involved |
| T-18-04-02 | Tampering | RoundDisplay quality labels | accept | Label array is a static constant evaluated from a numeric index; no user-controlled input reaches the label expression |
</threat_model>

<verification>
After both tasks complete, run:

1. Grep checks from each task's `<verify>` block (listed in each task above).

2. Manual smoke check (open the app):
   - Navigate to Help — confirm "2 players" in Before You Start
   - Confirm no bullet list under "Odd number of players"
   - Confirm bench clarification in Fixing Things
   - Confirm no "Change who sits out" paragraph
   - Confirm "Need to correct a played round?" paragraph present
   - Confirm slider relative-values note in Settings & Preferences
   - Generate a round, tap Alternatives — confirm labels read Best Match / Good Match / Okay Match (not Option 3)
</verification>

<success_criteria>
- All seven UAT gaps (GAP-01 through GAP-07) are closed
- No regression: file structure and exports are intact for both files
- grep checks listed in each task all pass
- No "Option N" fallback label in the alternatives panel
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-copy/18-04-SUMMARY.md`
summarizing what was changed in each file and confirming all seven gaps are closed.
</output>

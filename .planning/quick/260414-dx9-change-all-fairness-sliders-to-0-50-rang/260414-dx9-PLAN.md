---
phase: quick
plan: 260414-dx9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/Settings.js
autonomous: true
requirements: []
must_haves:
  truths:
    - All six sliders accept a value of 0 (slider can be dragged to the far left)
    - All six sliders have a maximum of 50
    - Setting any slider to 0 saves correctly and does not produce NaN
    - User-facing text is plain English with no developer jargon ("penalty", "weight")
    - Hint text under each slider explains the practical effect in organizer terms
  artifacts:
    - path: src/views/Settings.js
      provides: Updated slider ranges (0-50) and improved copy
  key_links:
    - from: slider input (min/max attributes)
      to: updateWeights() parseInt
      via: input event
      pattern: "parseInt.*value"
---

<objective>
Change all six fairness/optimization sliders in Settings from 1-50 to 0-50 range, and rewrite the user-facing text (section description, slider labels, hint text) to be clear plain-English for organizers rather than developer-facing penalty jargon.

Purpose: Organizers should be able to set a slider to 0 to fully disable a preference, and the text should communicate impact in practical terms (e.g., "how strongly to avoid...") not internal algorithm terms ("penalty").

Output: Updated src/views/Settings.js with min="0" on all six sliders and improved copy throughout the Scheduler Optimization section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update slider ranges to 0-50 and rewrite user-facing copy</name>
  <files>src/views/Settings.js</files>
  <action>
In src/views/Settings.js, make the following changes to the HTML template string and reset handler:

**Slider range change (6 sliders):**
Change `min="1"` to `min="0"` on all six range inputs:
- #weight-partner
- #weight-opponent
- #weight-sitout
- #weight-singles
- #weight-threeway-solo
- #weight-threeway-pair

The max="50" values are already correct — leave them unchanged.

**Section header copy:**
Change the section description from:
  "Adjust how the algorithm prioritizes variety vs fairness."
To:
  "Control how strongly the scheduler avoids repeating matchups. Drag to 0 to turn off a preference entirely."

**Hint text under each slider (replace the existing `text-[10px]` paragraphs):**

- Repeated Partners hint: "How strongly to avoid scheduling the same two players as partners again."
- Repeated Opponents hint: "How strongly to avoid scheduling the same two players against each other again."
- Fair Sitting Out hint: "How strongly to ensure every player sits out an equal number of rounds."
- Singles Match hint: "How strongly to avoid scheduling the same players in a 1v1 singles match again."
- 3-Way Solo hint: "How strongly to avoid putting the same player alone on a 3-player court again."
- 3-Way Pair hint: "How strongly to avoid repeating the same pair on the full-side of a 3-player court again."

**Slider labels (the bold `<label>` text):** These are already clear — leave them unchanged (Repeated Partners, Repeated Opponents, Fair Sitting Out, Singles Match, 3-Way Solo, 3-Way Pair).

**Reset defaults:** The reset handler sets hardcoded values (5, 10, 3, 15, 20, 15). These remain valid within 0-50 — leave them unchanged.

**NaN guard:** parseInt("0") returns 0 which is fine. No code change needed in updateWeights().

Do NOT change any other section (Backup & Restore, App Data, About).
  </action>
  <verify>
    <automated>grep -n 'min="0"' src/views/Settings.js | wc -l</automated>
  </verify>
  <done>
    - Output of grep shows exactly 6 lines with min="0"
    - Section description reads "Control how strongly the scheduler avoids repeating matchups..."
    - All six hint texts use "How strongly to avoid..." or "How strongly to ensure..." phrasing
    - No instance of min="1" remains in the file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Updated Settings page: all fairness sliders now range 0-50 with rewritten plain-English hint text.</what-built>
  <how-to-verify>
    1. Run the dev server: npm run dev
    2. Open the app in a browser and navigate to Settings
    3. Drag any slider all the way to the left — verify it reaches 0 (not 1) and the value badge shows "0"
    4. Read the section description beneath "Scheduler Optimization" heading — confirm it explains you can drag to 0 to disable
    5. Read the hint text under each slider — confirm it uses plain English (no "penalty" or "weight" jargon)
    6. Click "Reset to Defaults" and verify sliders snap back to their default values (5, 10, 3, 15, 20, 15) without errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
grep -c 'min="0"' src/views/Settings.js   # must output 6
grep 'min="1"' src/views/Settings.js       # must output nothing
grep 'How strongly' src/views/Settings.js  # must show 6 hint lines
</verification>

<success_criteria>
- All six sliders have min="0" max="50"
- No slider minimum is "1"
- Section description is updated to plain English explaining the 0 = off behaviour
- All six hint texts are written in organizer-friendly language without "penalty" or "weight" jargon
- Reset to Defaults still functions correctly (values 5, 10, 3, 15, 20, 15 are all within 0-50)
</success_criteria>

<output>
After completion, create `.planning/quick/260414-dx9-change-all-fairness-sliders-to-0-50-rang/260414-dx9-SUMMARY.md`
</output>
